<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SleepLab" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/sleep_navy</item>
    </style>

    <style name="Theme.SleepLab" parent="Base.Theme.SleepLab" />

    <style name="InputLayoutStyle" parent="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">80dp</item>
        <item name="boxStrokeColor">@color/white</item>
        <item name="hintTextColor">@color/gray</item>
        <item name="errorEnabled">true</item>
        <item name="errorIconDrawable">@null</item>
        <item name="helperTextEnabled">true</item>
        <item name="helperTextTextColor">@color/sleep_navy</item>
        <item name="colorError">@color/red</item>
    </style>

    <style name="myCardViewStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">18dp</item>
        <item name="android:layout_marginEnd">18dp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="cardElevation">3dp</item>
        <item name="cardCornerRadius">12dp</item>
    </style>

    <style name="cardViewStyle.myCardViewStyleFixed" parent="myCardViewStyle">
        <item name="android:layout_height">@dimen/cv_width</item>
    </style>
</resources>